# ===========================================
# API基础配置
# ===========================================
# API基础地址（不要以斜杠结尾）
API_BASE_URL=https://api.example.com

# ===========================================
# API认证配置（至少配置一种认证方式）
# ===========================================
# Bearer token认证（可选，优先使用）
API_ACCESS_TOKEN=eyJhbGaskjflk...

# 账号密码认证（使用账号密码认证时必需）
API_USERNAME=root
API_PASSWORD=your_password

# ===========================================
# 其他可选配置
# ===========================================
# 目标渠道状态（可选，默认0）
# 0: 所有状态
# 1: 启用
# 2: 手动关闭
# 3: 自动关闭
TARGET_CHANNEL_STATUS=0

# 更新渠道之间的随机休眠时间最大值（可选，默认3.0秒）
DELAY_TIME=3.0

# 客户端类型（可选，默认onehub）
# 可选值: onehub, oneapi, newapi
CLIENT_TYPE=onehub

# ===========================================
# 示例配置（已注释）
# ===========================================
#API_BASE_URL=https://api.example.com
#API_ACCESS_TOKEN=you_access_token
#API_USERNAME=admin
#API_PASSWORD=your_secure_password
#TARGET_CHANNEL_STATUS=1
#DELAY_TIME=2.0
#CLIENT_TYPE=onehub
